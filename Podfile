# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwinjectCalculator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwinjectCalculator
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'Swinject', '2.6.0'
  pod 'SwinjectStoryboard'

  target 'SwinjectCalculatorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwinjectCalculatorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
